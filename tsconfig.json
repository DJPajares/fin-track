{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["./shared/*"],
      "@web/*": ["./packages/web/*"], // Specific to the `web` package
      "@api/*": ["./packages/api/*"] // Specific to the `api` package
    }
  }
}
